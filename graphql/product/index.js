const typeDef = require('./product.typeDef');
const queries = require('./product.queries');
const mutations = require('./product.mutations');
const resolvers = require('./product.resolvers');

module.exports = { typeDef, queries, mutations, resolvers };
