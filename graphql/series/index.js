const typeDef = require('./series.typeDef');
const queries = require('./series.queries');
const mutations = require('./series.mutations');
const resolvers = require('./series.resolvers');

module.exports = { typeDef, queries, mutations, resolvers };
